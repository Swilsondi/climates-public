//TODO: It needs to:

// Handle register → creates user
// Handle login → checks user + issues JWT token
// Use bcrypt.compare() for login
// Use jsonwebtoken.sign() to create a token
// You’ll import your User model here and interact with it for user creation & login.
